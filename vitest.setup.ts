import { beforeAll } from "vitest";

// Define custom elements for testing
beforeAll(() => {
  // This ensures web components are properly registered
  // The actual components will be loaded from the built dist files
});
